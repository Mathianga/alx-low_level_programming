#include "main.h"

/**
 *_isdigit - a function that checks for a digit
 *@c: parameter to be computes
 *Return: alway 0
 */

int _isdigit(int c)
{
	c = 0;
	while (c < 10)
	{
		return (1);
		c++;
	}
	return (0);
}
